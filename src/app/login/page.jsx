import React from 'react';

export const metadata = {
  title: "StudyNook | Login",
};

const LoginPage = () => {
    return (
        <div>
            Login Page
        </div>
    );
};

export default LoginPage;